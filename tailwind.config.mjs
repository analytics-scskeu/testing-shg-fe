const config = {
    theme: {
        extend: {
            fontFamily: {
                gotham: ["var(--font-gotham)", "system-ui", "sans-serif"],
            },
            screens: {
                "3xl": "1650px",
            },
        },
    },
};

export default config;
